import { PortfolioLayout } from '../layout';
import { ProfileView } from '../views';

export const PortfolioPage = () => {
  return (
    <PortfolioLayout>
      <ProfileView />
    </PortfolioLayout>
  );
};
